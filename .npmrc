# Pin public registry explicitly — guards against dependency-confusion redirects
registry=https://registry.npmjs.org/

# Enforce declared packageManager / engines (no silent version drift)
engine-strict=true

# Warn if node_modules drifts from the lockfile before running scripts (tamper signal)
verify-deps-before-run=warn
